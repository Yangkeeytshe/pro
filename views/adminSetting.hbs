<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Admin Settings</title>
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
  />
  <style>
    html.dark-mode body {
  background-color: #000000;
  color: #f5f5f5;
}

    body {
      background-color: #f3f4f6;
      color: #111;
      transition: background-color 0.3s, color 0.3s;
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding-top: 60px; /* For fixed nav */
    }

    .dark-mode {
      background-color: #1e1e2f;
      color: #f5f5f5;
    }

    .dark-mode input,
    .dark-mode label,
    .dark-mode h2 {
      color: #f5f5f5;
    }

    .admin-settings-container {
      max-width: 1100px;
      margin: 40px auto 80px;
      background: url("productimage/sky1.jpg") no-repeat center center/cover;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      padding: 2rem;
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .admin-settings-card {
      background-color: #fff;
      border-radius: 12px;
      padding: 2rem;
      box-shadow: 0 8px 24px rgba(0,0,0,0.05);
    }

    .dark-mode .admin-settings-card {
      background-color: #2a2a40;
      box-shadow: 0 8px 24px rgba(255, 255, 255, 0.1);
    }

    .admin-settings-card h2 {
      font-size: 22px;
      color: #08509ee4;
      margin-bottom: 1.5rem;
      border-bottom: 2px solid #08509ee4;
      padding-bottom: 0.5rem;
    }

    .dark-mode .admin-settings-card h2 {
      color: #a3c9ff;
      border-color: #a3c9ff;
    }

    .admin-settings-card form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .admin-settings-card label {
      font-weight: 600;
      color: #374151;
    }

    .dark-mode .admin-settings-card label {
      color: #ccc;
    }

    .admin-settings-card input[type="text"],
    .admin-settings-card input[type="email"],
    .admin-settings-card input[type="password"] {
      padding: 10px 12px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 16px;
      width: 100%;
      box-sizing: border-box;
      background-color: #fff;
      color: #111;
      transition: background-color 0.3s, color 0.3s, border-color 0.3s;
    }

    .dark-mode .admin-settings-card input[type="text"],
    .dark-mode .admin-settings-card input[type="email"],
    .dark-mode .admin-settings-card input[type="password"] {
      background-color: #3a3a57;
      color: #f5f5f5;
      border-color: #666;
    }

    .admin-settings-card button {
      margin-top: 1rem;
      background-color: #007bff33;
      color: #130101;
      padding: 10px 22px;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: fit-content;
    }

    .admin-settings-card button:hover {
      background-color: #129cb5ad;
    }

    .preferences-options {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .preferences-options label {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 500;
      font-size: 15px;
      color: #374151;
    }

    .dark-toggle {
      cursor: pointer;
      padding: 0.5rem 1rem;
      background-color: #333;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      width: fit-content;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .dark-mode .dark-toggle {
      background-color: #eee;
      color: #111;
    }

    @media (max-width: 768px) {
      .admin-settings-container {
        padding: 1rem;
      }
    }

    .top-nav {
      top: 0;
      right: 0;
      height: 60px;
      background-color: #ffffff;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding: 0 2rem;
      z-index: 1200;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .dark-mode .top-nav {
      background-color: #2a2a40;
      color: #f5f5f5;
    }

    .nav-right a {
      margin-left: 1.5rem;
      text-decoration: none;
      font-size: 20px;
      color: #374151;
      transition: transform 0.2s ease, color 0.3s ease;
    }

    .dark-mode .nav-right a {
      color: #a3c9ff;
    }

    .nav-right a:hover {
      transform: scale(1.2);
      color: #2563eb;
    }
       @media (max-width: 768px) {
      .admin-layout {
        flex-direction: column;
      }
      .stat-boxes {
        flex-direction: column;
      }
    }
  .top-nav {
  position: fixed;
  top: 0;
  right: 30px;
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 20px;
  padding-right: 20px;
  z-index: 1000;
  background: transparent; /* No background */
}

.nav-right {
  display: flex;
  gap: 20px; /* or adjust to your desired spacing */
}


.nav-right a:hover {
  color: #2563eb;
  transform: scale(1.1);
}
  </style>
</head>
<body>
  <nav class="top-nav">
    <div class="nav-right">
      <a href="/admin/adminDashboard" title="Home"><i class="fas fa-house"></i></a>
      <a href="/logout" title="Logout"><i class="fas fa-right-from-bracket"></i></a>
      <a href="/admin/adminSetting" title="Settings"><i class="fas fa-gear"></i></a>
    </div>
  </nav>
  

  <main class="admin-settings-container">
    <h2>Admin Settings</h2>
     {{#if successMessage}}
    <div id="successMessage" style="
      background-color: #d4edda;
      color: #155724;
      padding: 1rem 1.5rem;
      border-radius: 6px;
      margin-bottom: 1.5rem;
      border: 1px solid #c3e6cb;
      font-weight: 600;
    ">
      {{successMessage}}
    </div>
  {{/if}}

    <section class="admin-settings-card">
      <form method="POST" action="/admin/updateProfile">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" value="{{admin.name}}" required />
        <br />

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="{{admin.email}}" readonly />
        <br />

        <button type="submit">Update</button>
      </form>
    </section>

    <section class="admin-settings-card">
      <h2>Change Password</h2>
      <form method="POST" action="/admin/changePassword">
        <input type="hidden" name="email" value="{{admin.email}}" />

        <label for="newPassword">New Password</label>
        <input type="password" id="newPassword" name="newPassword" required />

        <button type="submit">Change Password</button>
      </form>
    </section>

    <section class="admin-settings-card">
      <h2>Preferences</h2>
      <div class="preferences-options">
        <button class="dark-toggle" type="button" onclick="toggleDarkMode()">🌙 Dark Mode</button>
      </div>
    </section>
  </main>

  <script>
  function toggleDarkMode() {
  document.documentElement.classList.toggle('dark-mode');
  localStorage.setItem('darkMode', document.documentElement.classList.contains('dark-mode'));
}

window.addEventListener('DOMContentLoaded', () => {
  if (localStorage.getItem('darkMode') === 'true') {
    document.documentElement.classList.add('dark-mode');
  }
});
<script>
  function toggleSidebar() {
    const sidebar = document.getElementById("adminSidebar");
    sidebar.classList.toggle("hidden");
  }
</script>
  </script>
</body>
</html>
