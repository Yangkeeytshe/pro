{{> header}}

<style>
  body {
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    background-color: #f6f6f6;
    color: #1c1c1c;
    margin-top: 200px;
    padding: 0;
  }.forgot-wrapper {
  max-width: 550px;           /* medium width */
  margin: 100px auto;
  background-color: #fff;
  padding: 45px 35px;         /* comfortable padding */
  border-radius: 12px;
  box-shadow: 0 6px 20px rgba(0,0,0,0.08);
}

.forgot-wrapper h2 {
  font-size: 28px;            /* a bit larger heading */
  margin-bottom: 28px;
  color: #333;
}

.forgot-wrapper form {
  display: flex;
  flex-direction: column;
  gap: 22px;                  /* moderate spacing */
}

.forgot-wrapper input {
  padding: 14px 16px;         /* slightly larger inputs */
  font-size: 16px;            /* readable font */
  border: 1px solid #ccc;
  border-radius: 6px;
  background-color: #f9f9f9;
}

.forgot-wrapper button {
  background-color: #098abd5f;
  color: black;
  border: none;
  padding: 14px;
  font-size: 17px;
  border-radius: 6px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.forgot-wrapper button:hover {
  background-color: #0ab1c7;
}

.forgot-wrapper .message {
  text-align: center;
  margin-top: 18px;
  color: crimson;
  font-weight: 500;
  font-size: 15px;
}


</style>
<div class="forgot-wrapper">
  <h2>Reset Your Password</h2>
  <form action="/forgot-password" method="POST">
    <input type="hidden" name="token" value="{{token}}">
    <input type="email" name="email" placeholder="Enter your email" required>
    <button type="submit">Reset Password</button>
  </form>

  {{#if message}}
    <div class="message">{{message}}</div>
  {{/if}}
</div>

{{> footer}}
